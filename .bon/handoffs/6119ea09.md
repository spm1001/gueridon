# Handoff — 2026-03-28

session_id: 6119ea09-10f2-4d9b-bbaf-44748ea6e842
purpose: Fix bugs found by roadtesting compound-engineering review skills

## Done
- Installed and evaluated compound-engineering plugin (v2.56.1) — 41 skills, 47 agents
- Roadtested ce:review, document-review, and titans on same gueridon diff for comparison
- Fixed deviceMap cleanup bug in push.ts (3 code paths: remove, prune, send-expired)
- Fixed _testing.reset() to clear deviceMap, added getDeviceMap() for assertions
- Added 4 new tests covering deviceMap cleanup in all paths (681→685 tests)
- Added double-shutdown guard in bridge.ts (duplicate signal protection)
- Required VAPID_SUBJECT env var instead of silent fallback to admin@example.com
- Extracted buildSessionRecords() to DRY persistSessions + persistSessionsSyncWithSnapshot
- Deleted dead persistSessionsSync function (only WithSnapshot was ever called)
- Updated CLAUDE.md: MAX_SUBSCRIPTIONS 5→3, added device-based dedup mention
- Archived docs/decisions.md (stale — permissions/transport/hosting all changed, CLAUDE.md is authoritative)
- Pushed to main (610bff3)

## Gotchas
- VAPID_SUBJECT change: push now silently disables if env var missing (better than fake address, but verify /opt/gueridon/.env has it set before next restart)
- Changes NOT deployed to production — pushed to GitHub only, /opt/gueridon needs git pull + restart
- deviceMap cleanup is copy-pasted 3 times (iterate, find matching endpoint, delete, break) — if a 4th path appears, extract to removeDeviceByEndpoint()

## Risks
- Production deploy will change VAPID behaviour — confirm env var before restarting service
- The `break` after deviceMap.delete() inside for...of iteration is correct (JS Map allows delete during iteration) but looks suspicious — someone might "fix" it

## Next
- Deploy to production: `cd /opt/gueridon && git pull && npm install && sudo systemctl restart gueridon`
- Verify VAPID_SUBJECT is in /opt/gueridon/.env first
- Consider adopting CE skills for regular use: ce:plan (structured planning), ce:brainstorm (requirements), ce:review (pre-merge gate), context7 MCP (library docs)
- CE vs titans comparison doc (Someday/Maybe) — CE is forensic (confidence-gated, autofix-routed), titans is interpretive (vision, trajectory, craft). Complementary, not competing.
- CE's document-review found gueridon has no threat model for full-RCE mobile interface — worth thinking about

## Reflection
**Claude observed:** The real output of this session was the CE plugin evaluation, not the gueridon fixes — the fixes were a happy side-effect of roadtesting. Both review systems independently found the same core bug (deviceMap leak), which validates both. CE's confidence gating and autofix classification are ideas worth stealing for titans. The document-review on decisions.md was unexpectedly valuable — 6 reviewers unanimously agreed the doc should be archived, and the security-lens findings (no threat model, Tailscale-only auth) are real gaps worth addressing.
**User noted:** All made sense, no additions.
