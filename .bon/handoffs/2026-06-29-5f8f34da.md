# Handoff — 2026-06-29

session_id: 5f8f34da-8d7c-4969-8fae-8cb464399eca
purpose: Future B build — RC spawn path, URL capture, push-to-phone, trust resolved
format: fond-v1

## For the next Claude

### Done
- **`gdn-difoto`** — added a non-Vertex node-pty `--remote-control` spawn path (`spawnRemoteControl`, `rcSessions` map, `POST /launch/:folder` gated on `GUERIDON_ENABLE_RC`). Env strip lives in `buildRemoteControlEnv` (`bridge-logic.ts`, unit-tested). Added a **main-guard** so `bridge.ts` imports without booting (lets harnesses/tests drive the real functions without `reapOrphans` touching shared state). Verified at the real layer: Teams session, `/proc` zero Vertex, attachable claude.ai URL.
- **max-mode tidy** — `spawnCC`'s `.gueridon-billing=max` strip was incomplete (3 of 7 Vertex vars); routed through the canonical `VERTEX_ENV_VARS`. Vertex billing option deliberately preserved.
- **`gdn-senila`** — `extractClaudeAiUrl()` captures the claude.ai URL from the pty (ANSI-strip + last-match); `handleLaunch` awaits it (≤15s) and returns it. Verified: URL in the `/launch` response in ~3s.
- **`gdn-dofuza`** — `pushLaunchReady()` pushes the attach URL to the phone (sw.js opens it on tap), gated on `allClients.size===0`. Verified: real spawn → push fired with the url, intercepted at the webpush boundary (no real phone hit).
- **`gdn-tetepu` MOOT / `gdn-bekegu` DONE** — folder trust **cascades from the nearest trusted ancestor**; `~/repos` is trusted and `SCAN_ROOT=~/repos`, so all launches inherit trust. No seeding, no `~/.claude.json` write — the flagged shared-write hazard never existed.
- **Outcomes closed:** `gdn-pijuti`, `gdn-maroke`, `gdn-bekegu`. **Docs:** scoped CLAUDE.md update (RC path + main-guard + `/launch`); understanding.md trust bullet rewritten + build-progress note; `gdn-mupito` brief carries the RC-reaping + SIGHUP finding.
- 5 commits pushed (`a730759`..`979813e`); 712 tests; live `:3001` untouched throughout; **nothing deployed** (flag off in prod).

### Reflection
- The trust detour was the session's pivot: I'd over-trusted a docs subagent ("no subtree trust") and started building per-folder seeding; Sameer's "test it" — and his catch of my confounded `/tmp` test — reversed it. A whole sub-project deleted before it was built. (Bright-spot in self.md.)
- Division held: build the mechanism + verify each brick at the real layer (real spawns, `/proc`, intercepted push), one commit per brick.

### Risks
- **`pty.kill()` defaults to SIGHUP, which `claude` survives** — observed orphaned RC processes in test harnesses. The bridge `shutdown()` rc-kill relies on it; in prod `KillMode=control-group` backstops shutdown, but `gdn-mupito`'s idle-reaping must escalate SIGTERM→SIGKILL. (Captured in `gdn-mupito --how`.)
- **Push gate (`allClients.size===0`)** fire-path verified; if a future change keeps a client always-connected, launch pushes would silently never fire.
- **Trust cascade relies on `~/repos` staying trusted** — a fresh machine (rebuild) would block all launches until `~/repos` is trusted once (a setup step, → deploy-guide.md when Future B ships).
- **Teams rate limits** — RC sessions bill to Teams/MAX, not Vertex.

### Opportunities
- **`gdn-todidu`** (ready) — strip the launcher UI to folder-list + launch + URL. This is where dofuza's in-page tap-through lands: `/launch` already returns the url, so it's render-only.
- **`gdn-fuzeba`** (ready) — re-point the share-sheet/deposit flow at the RC path; pass the deposit as the initial prompt (mode (a), confirmed by `gdn-lohupa`).
- **`gdn-deloce`/`gdn-wimera`** — delete the streaming back-half + rewrite CLAUDE.md for the launcher. LAST, only after the RC path proves out in daily use (migrate, don't big-bang). These also retire `gdn-kuciku`/`gdn-hodoco`/the AskUser+gauge layer.
- **`gdn-mupito`** — standalone idle-reaping; now also covers RC sessions (see its brief).

## For Claudes to come

The launch-from-mobile loop is **proven end-to-end**: `claude --remote-control <folder>` spawned in a node-pty (Vertex-stripped → Teams, which is what the claude.ai relay attaches to) → capture the `claude.ai/code/session_…` URL from the pty → push it to the phone. Folder trust needs **no seeding**: CC trust **cascades from the nearest trusted ancestor directory** (empirically verified — a fresh folder under trusted `~/repos` spawns with no prompt *even when its own `hasTrustDialogAccepted` is false*; a control under untrusted `~` blocks). Two durable techniques: (1) the **main-guard** (`IS_ENTRYPOINT` around the `// -- Start --` block) makes `bridge.ts` importable without booting the server — essential for testing/harnessing the spawn path without `reapOrphans()` clobbering the shared `~/.config/gueridon` state. (2) For verifying a tool's *behaviour*, a real spawn beats a docs-research subagent (the agent claimed "no subtree trust" and was wrong on the surface that mattered). Full framing + remaining plan in understanding.md → "Future B".
