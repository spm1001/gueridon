# Handoff — 2026-03-30

session_id: 70522ea5-0b49-4c4e-b2ed-c3d6d016ffb6
purpose: Hierarchical folder scanning, VertexAI billing, ~/.claude write coaching

## Done
- Two-level folder scanning: container directories (no .git, children with .git) expand into parent/child names (e.g. batterie/gueridon)
- VertexAI billing: env vars pass through to CC spawn, CC_MODEL env var adds --model flag, configured in /opt/gueridon/.env
- ~/.claude write protection: empirically verified CC v2.1.87 auto-denies Write/Edit in -p mode, added system prompt coaching for Bash heredoc workaround
- Fixed VAPID_SUBJECT in production .env (was admin@example.com placeholder)
- Fixed SSE routing mismatch: basename() returned "gueridon" but scanFolders returns "batterie/gueridon" — deriveFolderName() now strips SCAN_ROOT prefix
- Added integration tests for nested folder scanning + SSE routing key matching
- Bumped to v0.4.0, deployed twice (feature + hotfix)
- Verified VertexAI auth works on hezza

## Gotchas
- folderName is a routing contract: SSE events carry it, client discards mismatches. Any code that sets folderName must match what scanFolders returns — grep for all folderName assignments when changing scan logic
- basename() still used in two log-only emissions (lines ~1730/1749 in bridge.ts) — cosmetic, not a routing issue
- classifyDir checks each child for .git — O(children) I/O per top-level dir on every scan. Fine for ~15 dirs, could slow with hundreds
- Production is now on VertexAI billing exclusively (mit-dev-362409). No fallback toggle — to revert, remove CLAUDE_CODE_USE_VERTEX from .env and restart

## Risks
- GCP quota/billing suspension would take down all Guéridon sessions with no automatic fallback
- Wider nested folder names (batterie/gueridon) might overflow mobile switcher CSS — not yet verified on device
- ADC token expiry would fail silently per-session — no bridge-level startup validation

## Next
- Check mobile switcher rendering for wider nested folder names
- Consider VertexAI startup validation (pre-flight auth check in spawnCC or at bridge start)
- .bon/understanding.md synthesis still pending — contribution from 2026-03-11 waiting (KillMode shutdown race insight)
- Existing bon items remain open — gdn-howibu (mockup fixtures), gdn-cuduci (subagent restart safety), gdn-pimime (subagent contamination observation), gdn-wobaku (AskUser context), gdn-juruwa (sensitive file approvals), gdn-cabicu (permission-denied handling)

## Reflection
**Claude observed:** The basename() routing bug was the session's real lesson — we changed the producer (scanFolders) without checking the consumer (session creation). The integration test we added after the fact would have caught it. The implicit contract between folderName in sessions and name in scanFolders was invisible until it broke. Also notable: CC's ~/.claude write protection in -p mode is a dead end for bridge-level interception — the permission check happens inside CC before any event reaches stdout, so system prompt coaching is genuinely the only available mechanism.
**User noted:** All made sense, no additions.
