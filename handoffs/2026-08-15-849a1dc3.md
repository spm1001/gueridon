# Handoff — 2026-08-15

session_id: 849a1dc3-2472-4153-b4a3-5d5e859c5bc0
purpose: Diagnose why a cross-wallet resume showed the wrong Teams seat; ship ~/notes into Guéridon's launcher (gdn-seroba); seed the wallet switchboard (gdn-jibudu)
format: fond-v1

## For the next Claude

### Done
- Diagnosed the "claudefc --resume shows Teams:sameer" report: the billing was fine — the statusline seat-label cache (`/tmp/.claude-seat-<session-id>`) was keyed on session id alone, and a resume keeps its id but not its config dir, so a family-seat resume replayed the sameer-seat label. Compounding it, the transcript's last message was a stale 429 ("org's monthly spend limit"), replayed on resume. Fixed the cache key to include the config dir (`~/.claude` commit 171f03e, pushed) and cleared the poisoned caches.
- Handed Sameer the fork-safe resume for his ~/notes conversation: `cd ~/notes && claudefc --resume f7f5942e-… --fork-session` — fork because the remote-control worker for that conversation is still alive and a same-id resume would put two writers on one JSONL.
- Discharged trousse-wunule's render-verify debt (deglacer skill's CLAUDE_CONFIG_DIR-blindness bullet confirmed rendering in a fresh commis-lane session; noted on the item) and completed its stale Toolmaking dispatch line.
- Shipped **gdn-seroba** end-to-end: `EXTRA_FOLDERS` env (colon-separated absolute paths) lists folders outside the scan tree in the launcher. Code at b2e768f, deployed to /opt with `EXTRA_FOLDERS=/home/modha/notes`, verified live — notes in `/repos` and `/folders` with 120 sessions discovered, creation still mints under ~/repos. Item closed, dispatch line completed.
- Close-out fix (this commit): extra folders joined the gdn-wuvujo one-driver contract — `sessionDisplayName` now names an extra folder by its basename so the roster name matches the launcher name and live-session hiding fires. 787 tests green.
- Filed and shaped **gdn-jibudu** (wallet switchboard outcome, top of board): full brief, Sameer's verbatim `--badly`, roster-seed note, and first action **gdn-himaba** (design pass). Dispatch line added to the Toolmaking queue.
- Filed **carte-sumugu** on ~/.claude's board: proposed one-sentence extension of the traps.md ambient-state row with the cross-wallet cache face.

### Reflection
- The user's report ("resume brings up Teams:sameer, blocked on quota") was two pieces of replayed history — a stale display cache and a stale error message — not current state. The instrument-limits reflex (verification.md) earned its keep: the first hour was commands, not theories.
- Sameer corrected an over-read: the two Teams seats share one org UUID, but spend limits can be per-member on separate reset cycles (docs confirmed) — "your org's monthly spend limit" in the 429 is looser wording than the billing reality.
- The wuvujo naming mismatch was found by asking "what does the guard actually match on" rather than assuming the new folder inherited existing protections. Worth doing for any feature that joins an existing contract late.

### Uncertain
- Which process wrote the poisoned 17:05 seat-cache (Desktop pick-up vs a first resume attempt) — unproven and deliberately left so; the fix keys on mechanism, not writer.

### Risks
- A full phone launch into ~/notes has never been fired — resolveFolder's extra-folder branch is unit-covered only. First real tap is the honest test; if it fails, journalctl -u gueridon and the roster will say why.
- While any live session sits in ~/notes (the phone worker usually does), the launcher now correctly HIDES notes from the launch list (one-driver-per-folder). That's design, not breakage — End the live session from the roster to free the folder. Expect "notes is missing" reports that are actually this.
- Left alone on purpose: the two long-running remote workers in ~/notes (quota-blocked, harmless; killing them ends the phone threads) and the managed statusline's hardcoded primary-seat tier read (upstream: mc-zoluce).

### Opportunities
- gdn-himaba is the next gueridon session's draw-down: design the wallet switchboard on top of the existing gdn-vagori roster; gdn-jibudu's brief carries the mechanism facts and Sameer's falsifier.
- carte-sumugu awaits a ~/.claude session's verdict on the traps.md row extension.
- gdn-sudacu (Teams-lane retirement) is due a decision — the week of real use started 2026-08-03 and the wallet-switchboard design will touch the same surfaces; deciding them together may save a rework.

## For Claudes to come

In Claude Code, a session transcript carries no account identity: the wallet is purely which config dir's credential the process spawned with, and this estate exploits that deliberately — `~/.claude-commis/projects` is a symlink into `~/.claude/projects`, one shared transcript farm visible to every seat. Three consequences, each demonstrated live on 2026-08-15: any per-session cache keyed on session id alone lies after a cross-wallet resume, because resume keeps the id but not the environment (the statusline seat label did exactly this); cross-wallet resume must `--fork-session`, because the original wallet's processes (remote workers, Desktop) may still hold the same transcript file; and anything that names a wallet must derive it at render time from `CLAUDE_CODE_SESSION_ID`-adjacent env, never from history. Corollary for Guéridon specifically: when a folder or session joins the launcher late (EXTRA_FOLDERS), check each existing contract it should inherit — trust cascade, one-driver hiding, creation root — by reading what the contract actually matches on; the wuvujo guard matched on display name, and the display name disagreed with the launcher name until told otherwise.
