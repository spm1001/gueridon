# Handoff — 2026-07-19

session_id: baf34141-79cd-44ca-83cc-b272a51824cc
purpose: Deployed End-foreign-session; planned + *shrank* session-graduation via a substrate-watch check; fixed & live-verified Symptom 2 (Vertex-fresh-launch) and a CC-daemon roster leak.
format: fond-v1

## For the next Claude

### Done
- **Deployed gdn-racuca** (End a foreign session by pid) to `/opt` @`4fbd025` + **phone tap-test verified** — a disposable `local` row's End armed a two-tap "Sure?", SIGTERM landed (pid confirmed dead, graceful/resumable), row cleared. Sameer: "worked well - nice simple design." (The deploy also carried **bon-cewemo** `getLatestHandoff`.)
- **`/plan` → gdn-ruvegu** "Phone-launched sessions graduate cleanly to a terminal session." A substrate-watch check (verified live) found **`claude agents` is now a full session manager** — enumerate/status/return/reply/delete/resume, incl. headless `-p` (8→9 test), and it hands you the uuid. So the plan **shrank two-thirds**: retired the sprawling **gdn-cepalu** into gdn-ruvegu + 3 children (**gdn-duhino DONE**, gdn-kozoha, gdn-pizoke); **dropped** `gdn ls` (claude agents is richer + an existing surface) and the **salvage-UUID-archaeology** (claude agents gives the uuid). Seeded understanding.md (substrate-watch verdict + three-axis frame + scope boundaries + key seams).
- **Fixed + live-verified gdn-duhino (Symptom 2).** Root cause: the bridge's `handleSession` **already** had a `"new"` mode; its trigger was orphaned when the switcher was retired (gdn-deloce), so every Vertex launch resumed. Fix (grep-the-substrate, not a refactor): `launch.html` `launchVertex` sets a one-shot `sessionStorage` `gdnFreshLaunch` flag; `index.html` consumes it on first hash-connect → `sessionId:"new"`. Regression test added (`bridge.integration.test.ts`). Committed `ec89393`, frontend deployed to `/opt` (no restart). **Controlled-twin live proof:** identical seed — control `POST {}` resumed the seed, launcher Vertex-launch got a fresh id.
- **Fixed + live-verified gdn-mimije.** The CC daemon (`claude daemon run …`, `comm=="claude"`, cwd `~`) leaked into the roster as an End-able row (Sameer's mystery "~" tile). Fix: `isDaemonCmdline` filter in `scanClaudeSessions` + `sessions.test.ts`. Committed, server-deployed (restart — cgroup verified clear first). Daemon alive but excluded (roster `~` rows 2→1).
- **Closed gdn-botife** — genuinely fixed by bon-cewemo (`scanFolders` is tested, 44 green, not vacuous).
- **Docs:** CLAUDE.md noted Vertex-fresh-launch + daemon exclusion; understanding.md annotated Symptom 2 DONE. `772` tests green.

### Reflection
- The night's shape was **verification, not volume** — three moments, all driven by Sameer's pushback: (1) the substrate-watch reversal that *shrank* the plan (shipping less felt like good taste, not deflation); (2) catching a premature "5/5 proven" — the click-through was **confounded** (all five repos were `/close`'d → they'd open fresh anyway), inverted by the handoff-check; (3) the **controlled-twin test** that actually settled Symptom 2. "The substrate ate most of it" is the win, not a loss.
- Two clean deploys with the cgroup-safety check run first each time (no repeat of the 2026-07-06 restart-killed-a-session scar).

### Uncertain
- **gdn-pizoke's adopt seam is UNVERIFIED and load-bearing:** does a **bridge-owned** `-p`/RC session, ended via gdn's SIGTERM, appear in `claude agents` as resumable? PROVEN for *terminal* sessions (the screenshot shows ended ones with `resume with: claude --resume …`) — NOT for bridge-owned. Test with one real gueridon session before building adopt on it.

### Risks
- The client fresh-launch wiring (`launch.html`→`index.html` sessionStorage) is **untested inline JS** — a future edit could silently break the Symptom-2 fix; only caught live this session. Covered as a class by **gdn-pefipi** (extract the launcher inline JS to a tested module).
- gdn-mimije's filter is `argv[1]==="daemon"` only. If CC changes how the daemon is invoked (e.g. a `--daemon` flag), or if `claude agents`/`claude mcp` ever run as `comm=="claude"`, they could leak into the roster. Only the daemon was observed/handled.

### Opportunities
- **gdn-kozoha** — self-label gdn launches with a billing-lane marker via `--name` (`claude -p`) + the `--remote-control` positional prefix, e.g. `📱vertex·owner/repo` / `📱teams·owner/repo`, so they're obvious in `claude agents`. VERIFY `--name` actually renders for a headless `-p` session (untested).
- **gdn-pizoke** — adopt: verify the seam above FIRST; then the smoothing is small (surface the resume line; gdn owns its own `-p` uuids). RC adopt may want `claude --remote-control --session-id <uuid>` at spawn (also kills the salvage-dig).
- Standing tickles: gdn-jafebe (not-connected state, outcome), gdn-hodoco (1M gauge), gdn-harili (.env template), gdn-lometu (spawned-session MCP access).

## For Claudes to come

Durable lesson (already in understanding.md's 2026-07-19 "Substrate watch" + "Plan frame" sections — synthesize from there): **`claude agents` now owns session management** — it enumerates every live `claude` session (terminal, background, headless `-p`), shows rich status (aiTitle/preview/state/age), and offers return/reply/delete + the `claude --resume` command. So gueridon's durable, unique role has narrowed to **launch (the moat) → release-for-adopt (only gueridon can free a bridge-owned process) → label its own launches**. **Before building any session-management feature in gueridon, check whether `claude agents` already does it** — tonight that check deleted two-thirds of a plan. And a `/proc` `comm=="claude"` scan catches infra too: the CC daemon (`claude daemon run`) must be filtered out (gdn-mimije) — the same caution applies to any future `claude agents`/`mcp` subcommand that runs under the same comm.
